# image-feature-extraction-glcm
Texture-based image feature extraction using GLCM (Gray Level Co-occurrence Matrix). Includes preprocessing, grayscale conversion, and CSV export of texture metrics such as contrast, energy, homogeneity, and correlation.
